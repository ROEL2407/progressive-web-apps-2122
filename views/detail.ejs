<!DOCTYPE html>
<html lang="nl">
  <%- include('./components/head.ejs') -%>
  <body>
    <header>
        <a href="/" id="back"><</a>
        <a href="/" id="refresh">
            <h1>Rijksmuseum</h1>
        </a>
        <button id="search_btn">
            <i class="fas fa-search"></i>
        </button>
    </header>
      <article class="infobox">
        <div>
            <% if(data.hasImage == true){ %> <img class="detailImage" src= <%-
            data.webImage.url.slice(0, -3) + "=s1000" %> alt="listenn to description"> <% } %>
        </div>
        <h2><%- data.title %></h2>
        <p><strong>Maker:</strong> <%- data.principalOrFirstMaker %></p><br />
        <p><strong>Beschrijving:</strong> <%- data.plaqueDescriptionDutch %></p>
        <p></p>
        </section>
      </article>
  </body>
</html>